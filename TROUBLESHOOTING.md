# 🔧 Gemini API 문제 해결 가이드

## 📌 현재 설정

- **모델**: `gemini-1.5-flash`
- **API Version**: `v1beta` (기본값)
- **SDK**: `@google/generative-ai` v0.21.0+

---

## ❓ 자주 발생하는 오류와 해결법

### 1️⃣ "404 Not Found - model not found for API version"

**원인**: 
- API 키가 v1beta를 지원하지 않음
- 구버전 API 키 사용

**해결법**:
```bash
# 1. 새로운 API 키 발급
# https://aistudio.google.com/app/apikey 접속
# "Create API Key" 클릭 → 새 프로젝트 생성 → 키 복사

# 2. .env 파일 업데이트
GEMINI_API_KEY=새로발급받은키

# 3. 테스트
npm run fetch-news
```

---

### 2️⃣ "API_KEY_INVALID" 또는 "invalid API key"

**원인**: 
- API 키가 잘못됨
- API 키에 공백이나 특수문자 포함

**해결법**:
```bash
# .env 파일 확인
cat .env

# API 키 형식: AIzaSy로 시작하는 39자
# 올바른 예: AIzaSyCEo1234567890abcdefghijklmnopqr
# 잘못된 예: AIzaSy... (불완전) 또는 공백 포함

# 새 키 발급:
# https://aistudio.google.com/app/apikey
```

---

### 3️⃣ "quota exceeded" 또는 "rate limit"

**원인**: 
- 무료 할당량 초과 (60회/분)
- 너무 많은 요청

**해결법**:
```bash
# 1. 대기 시간 늘리기
# scripts/fetchNews.js 파일에서
await new Promise(resolve => setTimeout(resolve, 10000)); # 5000 → 10000

# 2. 할당량 확인
# https://aistudio.google.com/app/apikey → 사용량 확인

# 3. 내일 다시 시도 (일일 한도 초과 시)
```

---

### 4️⃣ "JSON 파싱 실패" 또는 "필수 필드 누락"

**원인**: 
- AI가 JSON 형식을 지키지 않음
- 응답이 불완전함

**해결법**:
- 자동으로 재시도됨 (최대 3회)
- 계속 실패하면: 
  ```bash
  # 프롬프트 단순화 (scripts/fetchNews.js)
  # 또는 다른 뉴스로 시도
  ```

---

### 5️⃣ RSS 피드 오류

**원인**: 
- RSS 피드 사이트 접속 불가
- 타임아웃

**해결법**:
```javascript
// scripts/fetchNews.js에서 해당 피드 주석 처리
const RSS_FEEDS = {
  // '경제': 'https://www.mk.co.kr/rss/30100041/', // 주석
  '세계경제': 'https://www.hankyung.com/feed/economy',
  // ...
};
```

---

## ✅ 정상 작동 확인

### 성공 시 출력:
```
📰 뉴스 수집 시작...
🔑 API Key: AIzaSyCE...ESbM
🤖 사용 모델: gemini-1.5-flash
📡 경제 뉴스 가져오는 중...
   ✓ 수집된 항목: 50개
   ✓ 필터링 후: 2개
   🤖 AI 변환 중: ...
   ✅ 💰 청소년 용돈 관리...
   
📊 수집 완료 통계:
   ✅ 성공: 10개
   ❌ 실패: 0개
   📰 최종 선택: 10개
```

---

## 🆘 그래도 안 되면?

### 1. API 키 완전히 새로 발급

```bash
# 1. Google AI Studio 접속
https://aistudio.google.com/app/apikey

# 2. 기존 키 삭제
# 3. "Create API Key" 클릭
# 4. "Create API key in new project" 선택
# 5. 새 키 복사

# 6. .env 파일 업데이트
echo "GEMINI_API_KEY=새로운키" > .env

# 7. 테스트
npm run fetch-news
```

### 2. SDK 재설치

```bash
# node_modules 삭제
rm -rf node_modules package-lock.json

# 재설치
npm install

# 테스트
npm run fetch-news
```

### 3. Node.js 버전 확인

```bash
# Node.js 버전 확인
node --version

# v20.0.0 이상이어야 함
# 아니면 https://nodejs.org 에서 최신 LTS 설치
```

---

## 📞 추가 도움

문제가 계속되면:

1. **에러 메시지 전체 복사**
2. **다음 정보 함께 제공:**
   - Node.js 버전: `node --version`
   - SDK 버전: `npm list @google/generative-ai`
   - 에러 로그 전체

---

## 💡 팁

### API 키 보안
```bash
# .env 파일이 Git에 올라가지 않도록 확인
cat .gitignore

# 출력에 .env가 있어야 함
```

### 비용 절약
```bash
# 무료 할당량: 60 요청/분
# 하루 최대: 1500 요청

# 뉴스 수집 1회 = 약 10-16개 요청
# 하루 1회 실행이면 문제없음
```

### 테스트 실행
```bash
# 로컬에서 먼저 테스트
npm run fetch-news

# 성공하면 GitHub에 푸시
git add .
git commit -m "Test fetch news"
git push
```

---

**🎯 대부분의 문제는 API 키를 새로 발급받으면 해결됩니다!**
